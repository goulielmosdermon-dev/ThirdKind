import {
  aboutSection,
  article,
  contactInfo,
  project,
  siteSettings,
} from './documents';
import { blockContent, canvasPosition, credit, seo, videoUrl } from './objects';

export const schemaTypes = [
  canvasPosition,
  seo,
  credit,
  videoUrl,
  blockContent,
  project,
  article,
  aboutSection,
  contactInfo,
  siteSettings,
];
